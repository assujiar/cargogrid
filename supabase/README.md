# Supabase

Supabase project files and migrations live here. Phase 00 intentionally creates no database tables, logistics schema, tenant schema, RBAC schema, or RLS policies.

Future tenant-scoped migrations must include `tenant_id`, RLS policies, indexes, and regression coverage.
